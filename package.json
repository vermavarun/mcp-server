{
  "name": "notes-mcp-server",
  "version": "1.0.0",
  "description": "A simple MCP server for managing notes - educational example",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "notes-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "dev": "tsc && node build/index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "notes",
    "server"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
